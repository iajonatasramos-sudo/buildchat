node_modules/
dist/
.next/
.DS_Store
*.log

# Build local do painel e pacote de deploy
painel/.next/
painel/node_modules/
painel/.env.local
painel.zip

# DADOS PESSOAIS — não versionar.
# Contém 188 números de telefone reais e as mídias da clínica (áudios, PDFs),
# usados apenas na migração local do acervo. Mantenha a cópia no seu computador.
public/seed/
