{
  "name": "buildchat-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Servidor do BuildChat (SaaS) — schema, RLS e testes de isolamento",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.17"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
